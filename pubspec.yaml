name: nested
description: A Flutter Widget which helps nest multiple widgets without needing to manually nest them.
version: 0.1.0
author: Felix Angelov <felangelov@gmail.com>
repository: https://github.com/felangel/nested

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
